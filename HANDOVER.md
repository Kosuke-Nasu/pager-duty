# PitchDuty 引き継ぎマニュアル

感染症内科ピッチ当番表アプリ「PitchDuty」の運用・保守を引き継ぐ方向けの資料です。
**コーディング支援AI（Claude Code 等）を使わず、人の手だけで運用・改修することを前提**に書いています。

作成日：2026-08-04

---

## 0. 引き継ぎ時に必ず埋めてください（前任者が記入）

このマニュアルだけでは分からない、アカウント・URL などの情報です。
**引き継ぎの席でここを埋めてから渡してください。**

| 項目 | 内容 |
|---|---|
| アプリの公開URL（利用者が開くアドレス） | 【要記入】 |
| 公開方法（GitHub Pages／社内サーバ／その他） | 【要記入】 |
| GitHub アカウント（リポジトリ所有者） | Kosuke-Nasu ／ 引き継ぎ後の所有者：【要記入】 |
| GitHub リポジトリ | https://github.com/Kosuke-Nasu/pager-duty |
| Firebase プロジェクト名 / ID | pager-duty-28873 |
| Firebase コンソールにログインする Google アカウント | 【要記入】 |
| Firebase の課金プラン | 【要記入】（通常は無料の Spark プランで足ります） |
| 現在の利用部署・利用者数 | 【要記入】 |
| 問い合わせ先（利用者からの連絡窓口） | 【要記入】 |

> **最重要**：Firebase コンソールにログインできる Google アカウントを引き継がないと、
> データのバックアップも復旧もできなくなります。必ず新担当者を「編集者」または
> 「オーナー」として Firebase プロジェクトに追加してもらってください（手順は §7.1）。

---

## 1. このアプリは何か

- 感染症内科の**ピッチ（PHS）当番表**を、平日（月〜金）について自動作成・共有するWebアプリ。
- ブラウザで開くだけで使える。インストール不要。スマホではホーム画面に追加してアプリ風に使える（PWA）。
- **ログインなし**。URLを知っている人は誰でも閲覧・編集できる。
- データはインターネット上の Firebase（Google のデータ保存サービス）に置かれ、全員で同じ内容を共有する。
  誰かが変更すると、開いている他の人の画面にも数秒で反映される。

### 画面の構成（3つのタブ）

| タブ | 内容 |
|---|---|
| **当番表** | 月表示カレンダー、年間一覧（印刷可）、代役ルーレット、担当日数の集計、年度まるごとの自動作成・削除 |
| **メンバー登録** | 名前・外勤日・感染制御部ローテ期間・細菌検査室ローテ期間の登録、当番対象のON/OFF |
| **ルール** | 当番表の作成ルールの説明（**利用者向けの説明はこのタブが正本**。運用の質問が来たらまずここを案内） |

> アプリの**使い方**そのものは、アプリ内「ルール」タブに日本語で全部書いてあります。
> このマニュアルは、その裏側（**壊れたときの直しかた・設定の変えかた**）を扱います。

---

## 2. ファイル構成

リポジトリ直下に7ファイルだけ。ビルド作業やインストール作業は**一切不要**です。

| ファイル | 役割 | 触る頻度 |
|---|---|---|
| `index.html` | **アプリ本体。HTML・CSS・JavaScript が全部この1ファイルに入っています**（約1,280行） | 改修時はほぼここだけ |
| `firestore.rules` | Firebase 側のアクセス制御ルールの**控え**。※このファイルを置いただけでは反映されません（§7.4） | ほぼ触らない |
| `manifest.json` | ホーム画面に追加したときのアプリ名・アイコン設定 | ほぼ触らない |
| `sw.js` | ホーム画面追加を可能にするための最小のサービスワーカー | 触らない |
| `icon-180.png` / `icon-192.png` / `icon-512.png` | アプリのアイコン | 変更したいときだけ |
| `HANDOVER.md` | この資料 | 引き継ぎのたびに更新 |

### 技術的な前提（知っておくと安心なだけの情報）

- **フレームワークなし・ビルドなし**。`index.html` をブラウザで開けばそのまま動きます。
- Node.js も npm も使いません。`npm install` のような準備は不要です。
- Firebase の機能は、CDN から直接読み込んでいます（`index.html` 内の
  `https://www.gstatic.com/firebasejs/10.12.2/...` の行）。バージョンは 10.12.2 に固定。

---

## 3. 日常の運用（コードを触らない作業）

ここは**プログラムの知識ゼロでできる**作業です。

### 3-1. 年度が変わったとき

1. 当番表タブ上部の「年度」を新しい年度に切り替える。
2. 「年度まるごとの作成・削除」の**「作成を始める月」**を確認する（通常は4月）。
3. **「この年度を自動作成」**を押す。
4. 未割当や警告の件数がアラートで出るので、月表示から手動で調整する。

> 「この年度を自動作成」は、**その年度の手動調整も含めて全部作り直します**。他の年度には影響しません。
> 元に戻せないので、実行前に §5 のバックアップを取っておくと安全です。

### 3-2. メンバーの入れ替え

- 入職：メンバー登録タブで追加 → 「当番対象」にチェック。
- 異動・退職：**削除せず「当番対象」のチェックを外すだけ**を推奨します。
  削除すると、その人に割り当て済みの枠がすべて空欄になり、過去の当番表も崩れます。
- ローテ期間や外勤日が変わったら「編集」から更新。**更新後に自動作成し直さない限り、既存の割り当ては変わりません**。

### 3-3. 急な休み・長期休暇

当番表タブの「代役ルーレット」を使います。詳しい仕様はアプリ内「ルール」タブの §9 を参照。
**「この結果を反映する」を押すまでは当番表は変わりません。**

### 3-4. 印刷・配布

年間一覧カードの「印刷 / PDF保存」ボタン → A4縦で年間一覧の表だけが出ます。
画面のヘッダーやタブ、他のカードは印刷されません（印刷用CSSで制御しています）。

---

## 4. 変更を反映する手順（改修のしかた）

### 4-1. いちばん簡単な方法：GitHub のウェブ画面で直接編集

パソコンに何もインストールせずに変更できます。

1. https://github.com/Kosuke-Nasu/pager-duty を開く。
2. `index.html` をクリック → 右上の **鉛筆アイコン（Edit this file）**。
3. 直したい箇所を編集。
4. 下の「Commit changes」で、変更内容の説明を書いて保存。
5. GitHub Pages で公開している場合、**1〜3分後**に公開URLへ反映されます。

### 4-2. 手元のパソコンで編集する方法

```bash
git clone https://github.com/Kosuke-Nasu/pager-duty.git
cd pager-duty
# index.html をテキストエディタ（VS Code、メモ帳など）で編集
git add index.html
git commit -m "変更内容の説明"
git push
```

### 4-3. 変更前に必ず動作確認する

公開前に、**手元で `index.html` をダブルクリックしてブラウザで開けば動きます**。
ただしその状態でも**本番の共有データにつながっている**ため、
データを書き換える操作（自動作成・削除・保存）はしないでください。表示確認だけに使ってください。

> 安全に試したいときは、`index.html` のコピーを作り、`FIREBASE_CONFIG` の `apiKey` の値を
> `"ここに貼り付け"` に書き換えます。すると黄色い帯が出て**お試しモード**（保存されず、
> 共有もされない）になり、自由に触れます。確認が終わったらそのコピーは捨ててください。

### 4-4. 元に戻したいとき

GitHub のリポジトリ → 「Commits」で過去の履歴を開き、目的のコミットの
「Browse files」から当時の `index.html` の内容をコピーして貼り直すのが、いちばん確実です。

---

## 5. データのバックアップと復元（最重要）

**当番表とメンバー情報はすべて Firebase 上の「たった1つの書類」に入っています。**
アプリの操作ミスや削除ボタンで消えても、GitHub のコードを戻しても**データは戻りません**。

### 5-1. データの置き場所

Firebase コンソール → Firestore Database → コレクション `pitchduty` → ドキュメント `main`
→ フィールド **`json`**（データ全部が1本の長い文字列として入っています）

### 5-2. バックアップ手順（月1回・年度作成の直前は必須）

1. Firebase コンソール（https://console.firebase.google.com/）にログイン。
2. プロジェクト `pager-duty-28873` を選択。
3. 左メニュー「Firestore Database」→ `pitchduty` → `main` を開く。
4. `json` フィールドの値を**すべて選択してコピー**。
5. 日付を入れたテキストファイル（例：`pitchduty-backup-2026-08-04.txt`）に貼り付けて保存。

### 5-3. 復元手順

1. 上と同じ画面で `json` フィールドを開く。
2. 中身をバックアップした文字列で**まるごと置き換えて保存**。
3. アプリを開いているブラウザを再読み込みすると、その時点の状態に戻ります。

### 5-4. データの中身（参考）

`json` の中身はこういう形をしています。読めれば手直しも可能です。

```json
{
  "people": [
    {
      "id": "a1b2c3d4",
      "name": "山田",
      "gaikin": [2, 4],
      "ics": [{ "s": "2026-05-01", "e": "2026-05-31" }],
      "lab1": "2026-09-01",
      "lab2": "2026-09-30",
      "active": true
    }
  ],
  "schedules": {
    "2026": { "2026-04-06": "a1b2c3d4", "2026-04-07": "" }
  },
  "genStart": { "2026": 8 }
}
```

| キー | 意味 |
|---|---|
| `people[].gaikin` | 外勤曜日。1=月, 2=火, 3=水, 4=木, 5=金 |
| `people[].ics` | 感染制御部ローテ期間の配列（複数可）。`s`=開始日, `e`=終了日 |
| `people[].lab1/lab2` | 細菌検査室ローテの開始日／終了日 |
| `people[].active` | 当番対象かどうか |
| `schedules["年度"]["日付"]` | その日の担当者ID。`""`（空文字）は未割当 |
| `genStart["年度"]` | その年度の「作成を始める月」 |

---

## 6. よく変えたくなる設定と、その場所

すべて `index.html` の中にあります。**行番号は編集でずれるので、下の「検索する文字列」で探してください。**

### 6-1. 当番のルールに関する数値

検索文字列：`const WEEK_CAP`

```js
const WEEK_CAP=2;           // 1人あたりの週の担当回数の上限
const BALANCE_TOL=1;        // 担当日数の「最多−最少」をここまで詰める
const FY_MIN=2026;          // 年度の選択肢の下限（このアプリの運用開始年度）
const FY_AHEAD=5;           // 今年度から何年度先まで選べるようにするか
```

- `WEEK_CAP` を 3 にすれば「週3回まで」になります。
  **アプリ内「ルール」タブの表示も自動で追従します**（数値を埋め込む作りになっているため）。
- `BALANCE_TOL` を 0 にすると担当日数を完全に揃えようとしますが、
  条件を満たせず処理が途中で止まり、かえって偏る場合があります。1 のままを推奨。
- `FY_MIN` は運用開始年度なので**変更不要**。過去年度を選べるようにしたいときだけ下げます。

### 6-2. 「作成を始める月」の初期値

検索文字列：`const DEFAULT_GEN_START`

```js
const DEFAULT_GEN_START={2026:8};  // 初回のみ使う既定値
```

**すでに運用が始まっている今、ここを変えても効果はありません。**
（この値は、Firebase にデータが1件も無い最初の1回だけ使われます。
以後の設定は画面の「作成を始める月」から変更し、Firebase 側に保存されます。）

### 6-3. 名前の色

検索文字列：`const COLORS`
10色の配列です。メンバーの登録順に割り当てられます。色を足せば11人目以降も別色になります。

### 6-4. 祝日

検索文字列：`function holidays`

祝日は**日本の祝日法をコードで再現して自動計算**しています（振替休日・国民の休日も自動）。
カレンダーを毎年更新する必要はありません。ただし **次の場合は手作業での修正が必要**です（§9-1参照）。

- 祝日法が改正され、新しい祝日ができた／日付が変わったとき
- オリンピック特例のような**その年だけの移動**があるとき（現状は対応していません）

追加のしかたは、関数内の既存の行にならいます。

```js
add(1,1,'元日');            // 1月1日固定の祝日
add(7,nthMon(7,3),'海の日'); // 7月の第3月曜
```

### 6-5. アプリの見た目（色）

検索文字列：`:root{`
`--navy`（ヘッダーの濃紺）、`--blue`（ボタンの青）などを変えると全体の配色が変わります。

### 6-6. Firebase の接続先

検索文字列：`FIREBASE_CONFIG`

**Firebase プロジェクトを新しく作り直したとき以外、絶対に触らないでください。**
値を間違えるとアプリ全体がデータにつながらなくなります。

---

## 7. Firebase まわりの管理

### 7-1. 新しい担当者にアクセス権を渡す

1. Firebase コンソール → プロジェクト `pager-duty-28873` を開く。
2. 左上の歯車 → 「ユーザーと権限」。
3. 「メンバーを追加」→ 新担当者の Google アカウントを入力 → ロールは**「編集者」**（引き継ぎ完了なら「オーナー」）。

### 7-2. 料金

無料の Spark プランの範囲でまず問題ありません（1日あたり読み取り5万回・書き込み2万回まで）。
このアプリは 1ドキュメントの読み書きしかしないため、部署単位の利用で上限に達することはまずありません。
心配なら Firebase コンソールの「使用量と請求額」で月1回確認してください。

### 7-3. セキュリティの現状（利用者への周知が必要）

- **認証が一切ありません。URLを知っている人は誰でも閲覧・編集できます。**
- Firebase の `apiKey` は `index.html` に直接書かれています。これは Firebase の仕様上、
  公開されても問題ない種類の鍵ですが、**アクセスルール自体が全開放なので、
  実質的にURLを知る人は誰でも編集できる状態**です。
- そのため**氏名と当番以外の情報（患者情報など）を絶対に入力させないでください**。
  この注意はアプリ内（メンバー登録タブ上部・ルールタブ §11）にも表示されています。
- リポジトリが公開設定の場合、`index.html` の中身は誰でも読めます。
  → 引き継ぎ時に、リポジトリを Private にするか検討してください。
  （※ GitHub Pages で公開している場合、無料アカウントでは Private にすると公開が止まります）

### 7-4. アクセスルールを変えるとき

`firestore.rules` は**あくまで控え**です。ファイルを編集して push しても反映されません。

反映手順：Firebase コンソール → Firestore Database → 「ルール」タブ →
`firestore.rules` の中身を貼り付け → **「公開」** を押す。

現在のルールは、
- `pitchduty/main` の**読み取りは全開放**
- 書き込みは `json`（50万文字未満の文字列）と `updatedAt`（数値）の2項目だけに限定
- ドキュメントの**削除は拒否**
- それ以外のパスは全拒否

となっています。無関係な巨大データの投げ込みを防ぐための最低限の防御です。

---

## 8. トラブルシューティング

| 症状 | 原因と対処 |
|---|---|
| 画面右上に「読み込みエラー」「保存エラー」と出る | Firebase 側の問題かネットワーク断。まずページを再読み込み。続くなら Firebase コンソールでルールが誤って書き換えられていないか確認（§7.4） |
| 黄色い帯「お試しモードです」が出て保存されない | `index.html` の `FIREBASE_CONFIG` の `apiKey` が正しく入っていない。GitHub の履歴から正しい値に戻す |
| 変更したのに他の人の画面が古いまま | その人にページの再読み込みを依頼。スマホでホーム画面から起動している場合は、いったんアプリを閉じて開き直す |
| コードを直したのに公開URLに反映されない | GitHub Pages の反映に数分かかる。それでも変わらなければブラウザのキャッシュ削除（Ctrl+Shift+R）。GitHub の Actions タブでデプロイ失敗が出ていないかも確認 |
| 自動作成で「未割当」が大量に出る | 当番対象のチェックが付いた人が少ない、または外勤日・ローテ期間の登録が重なりすぎている。メンバー登録タブを見直す |
| 手動で直した内容が消えた | 「この年度を自動作成」を押すと手動調整も全部作り直されます。§5 のバックアップから復元 |
| 誰かの変更が消えた | **2人が同時に別の編集をすると、後に保存したほうで上書きされます**（§9-2）。復元は §5 |
| 祝日なのに当番が入っている | §9-1 の制限。`holidays` 関数の修正が必要（§6-4） |

---

## 9. 既知の制限・注意点

引き継ぎ後に「バグでは？」と言われがちな、**仕様として分かっている点**です。

### 9-1. 祝日は法改正に追随しません
祝日は計算式で自動生成しています。祝日法の改正や、オリンピック特例のような
**その年だけの移動には対応していません**。該当する年が来たら `holidays` 関数を手で直す必要があります（§6-4）。

### 9-2. 同時編集は「後勝ち」です
データはまるごと1つの書類として保存されます。2人が同時刻に別々の編集をすると、
**後に保存したほうの内容で全体が上書きされ、先の変更は消えます**。
自動作成のような大きな操作をするときは、他の人が編集していないタイミングを選んでください。

### 9-3. 元に戻す機能はありません
「自動作成」「年度の削除」「ルーレットの反映」「メンバー削除」はいずれも取り消せません。
アプリ側で確認ダイアログは出しますが、**バックアップ（§5）だけが唯一の保険**です。

### 9-4. 手動調整はルールを無視できます
カレンダーの日付をタップして行う手動調整には、外勤日・ローテ期間のチェックがかかりません
（選択肢に「外勤日」「ローテ中」の注記は出ます）。意図的な例外を作れる代わりに、
ルール違反の割り当ても作れてしまいます。作成後はカレンダー上の赤い警告表示
（「ローテ期間中!」「外勤日!」）を確認してください。

### 9-5. 集計は「作成を始める月」以降だけ
担当日数の集計は、その年度の「作成を始める月」より前の分を数えません。均等化の対象範囲と揃えるためです。

### 9-6. 年度の選択肢は 2026 年度から
`FY_MIN=2026` のため、2025年度以前は選べません（§6-1）。

### 9-7. Firebase のバージョンは 10.12.2 固定
CDN のURLで固定しています。動いているうちは**上げないでください**。
Firebase 側が古いバージョンの配信を止めた場合のみ、URL内のバージョン番号を上げて動作確認します。

---

## 10. 引き継ぎチェックリスト

引き継ぎの席で、この順に確認してください。

- [ ] §0 の表をすべて埋めた
- [ ] 新担当者が **Firebase コンソール**にログインでき、`pager-duty-28873` を開ける（§7.1）
- [ ] 新担当者が **GitHub リポジトリ**に書き込み権限を持っている（Settings → Collaborators）
- [ ] 新担当者が **公開URL**を知っており、実際にブラウザで開ける
- [ ] §5 のバックアップ手順を、新担当者が**自分の手で1回やってみた**
- [ ] §4-1 のコード変更手順を、新担当者が**軽微な変更で1回やってみた**（例：この資料の更新をコミットする）
- [ ] アプリ内「ルール」タブの内容を、新担当者が一読した
- [ ] §9 の制限事項（特に 9-2 の同時編集と 9-3 の取り消し不可）を新担当者が理解した
- [ ] 利用者からの問い合わせ窓口を新担当者に切り替え、利用部署に周知した
- [ ] 「氏名と当番以外の情報を入力しない」ルールを、利用部署に改めて周知した

---

## 11. 困ったときの参照先

| 内容 | 参照先 |
|---|---|
| アプリの使い方・当番決定ルール | アプリ内「ルール」タブ |
| 過去の変更履歴 | GitHub リポジトリの「Commits」 |
| コード内の細かい仕様 | `index.html` 内の日本語コメント（各処理の頭に説明があります） |
| Firebase の操作方法 | https://firebase.google.com/docs/firestore （日本語表示あり） |
| GitHub Pages の設定 | リポジトリ → Settings → Pages |
